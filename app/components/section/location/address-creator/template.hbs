<input
  type="text"
  name="street"
  class="street"
  value="{{model.street}}"
  oninput={{action 'fieldChanged' 'street'}}
  onchange={{action 'fieldChanged' 'street'}}
  onblur={{action saveAddress}}>

<input
  type="text"
  name="city"
  class="city"
  value="{{model.city}}"
  oninput={{action 'fieldChanged' 'city'}}
  onchange={{action 'fieldChanged' 'city'}}
  onblur={{action saveAddress}}>

<input
  type="text"
  name="state"
  class="state"
  value="{{model.state}}"
  oninput={{action 'fieldChanged' 'state'}}
  onchange={{action 'fieldChanged' 'state'}}
  onblur={{action saveAddress}}>

<input
  type="text"
  name="zip"
  class="zip"
  value="{{model.zip}}"
  oninput={{action 'fieldChanged' 'zip'}}
  onchange={{action 'fieldChanged' 'zip'}}
  onblur={{action saveAddress}}>

<input
  type="text"
  name="lat"
  class="lat"
  value="{{model.lat}}"
  oninput={{action 'fieldChanged' 'lat'}}
  onchange={{action 'fieldChanged' 'lat'}}
  onblur={{action saveAddress}}>

<input
  type="text"
  name="lon"
  class="lon"
  value="{{model.lon}}"
  oninput={{action 'fieldChanged' 'lon'}}
  onchange={{action 'fieldChanged' 'lon'}}
  onblur={{action saveAddress}}>
