<input
  type="text"
  name="street"
  class="street"
  value="{{model.street}}"
  oninput={{action 'fieldChanged' 'street'}}
  onchange={{action 'fieldChanged' 'street'}}
  onblur={{action save}}>

<input
  type="text"
  name="city"
  class="city"
  value="{{model.city}}"
  oninput={{action 'fieldChanged' 'city'}}
  onchange={{action 'fieldChanged' 'city'}}
  onblur={{action save}}>

<input
  type="text"
  name="state"
  class="state"
  value="{{model.state}}"
  oninput={{action 'fieldChanged' 'state'}}
  onchange={{action 'fieldChanged' 'state'}}
  onblur={{action save}}>

<input
  type="text"
  name="zip"
  class="zip"
  value="{{model.zip}}"
  oninput={{action 'fieldChanged' 'zip'}}
  onchange={{action 'fieldChanged' 'zip'}}
  onblur={{action save}}>

<input
  type="text"
  name="lat"
  class="lat"
  value="{{model.lat}}"
  oninput={{action 'fieldChanged' 'lat'}}
  onchange={{action 'fieldChanged' 'lat'}}
  onblur={{action save}}>

<input
  type="text"
  name="lon"
  class="lon"
  value="{{model.lng}}"
  oninput={{action 'fieldChanged' 'lng'}}
  onchange={{action 'fieldChanged' 'lng'}}
  onblur={{action save}}>
