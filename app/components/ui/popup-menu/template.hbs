{{ui/icon-button
  class='trigger'
  type='menu'
  size=1.4
  flat=true
  color=colors.DARK_GREY
  backgroundColor=colors.BABY_BLUE
  click=(action 'toggle')}}

{{#if showMenu}}
  {{#tether-dialog
      hasOverlay=false
      attachment='top left'
      targetAttachment='right left'
      target='.trigger'}}
    {{yield}}
  {{/tether-dialog}}
{{/if}}
