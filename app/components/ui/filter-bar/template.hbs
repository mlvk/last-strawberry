<input type="text" name="filter" class="filter" value={{value}} placeholder={{placeholder}}
  oninput={{action "onChange"}} onchange={{action "onChange"}}>
{{ui/icon-button
  flat=true
  type="clear"
  click=(action "clear")
}}