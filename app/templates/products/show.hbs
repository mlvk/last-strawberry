{{sections/products/product-editor
    model=model
    save=(route-action "save")
    destroyProduct=(route-action "destroyProduct")
    reset=(route-action "reset")
    validateProperty=(route-action "validateProperty")
}}
