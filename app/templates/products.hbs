{{sections/items/item-table
    items=products
    isProduct=true
    archiveItem=(route-action "archiveItem")
    saveItem=(route-action "saveItem")
    createNewProduct=(route-action "createNewProduct")}}
