import { rpIcon } from 'last-strawberry/helpers/rp-icon';
import { module, test } from 'qunit';

module('Unit | Helper | rp-icon');

// Replace this with your real tests.
test('it works', function(assert) {

  let result = rpIcon([42]);
  assert.ok(result);
});
