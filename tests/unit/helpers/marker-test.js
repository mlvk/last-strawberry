import { marker } from 'last-strawberry/helpers/marker';
import { module, test } from 'qunit';

module('Unit | Helper | marker');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = marker([42]);
  assert.ok(result);
});
