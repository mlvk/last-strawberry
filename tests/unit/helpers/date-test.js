
import { date } from 'last-strawberry/helpers/date';
import { module, test } from 'qunit';

module('Unit | Helper | date');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = date([42]);
  assert.ok(result);
});

