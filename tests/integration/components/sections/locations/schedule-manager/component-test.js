import { moduleForComponent, test } from 'ember-qunit';
import hbs from 'htmlbars-inline-precompile';

moduleForComponent('sections/locations/schedule-manager', 'Integration | Component | sections/locations/schedule manager', {
  integration: true
});

test('it renders', function(assert) {
  assert.expect(0);
  this.render(hbs`{{sections/locations/schedule-manager}}`);

});
